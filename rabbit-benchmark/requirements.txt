pika>=1.3
aio-pika>=9.4
numpy>=1.26
pandas>=2.2
plotly>=5.20
kaleido>=0.2
jinja2>=3.1
weasyprint>=61
psutil>=5.9
pytest>=8.0
pytest-asyncio>=0.23
# The hs-rabbit-client library (benchmarked as the `simple` client), installed
# editable from the sibling checkout. Kept here (not in pyproject.toml)
# because PEP 508 dependencies cannot express editable/relative-path installs.
-e ../rabbit-client-python
